# Changes to Slicer TMS
- modified so it can take environment variables instead of localhost
- eth0 does not have enough space to transfer all of the python data
    added chunked transmission and error checking