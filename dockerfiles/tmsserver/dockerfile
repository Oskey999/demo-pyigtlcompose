#docker build  -p 18944:18944 -p 18945:18945 -f ./dockerfiles/tmsserver/dockerfile .
#docker build -t test -f .\dockerfiles\tmsserver\dockerfile . 
#
# For GPU support, run with:
#docker run --gpus all -p 18944:18944 -p 18945:18945 -it test
# (requires nvidia-docker or Docker with GPU support enabled)

# Using NVIDIA CUDA runtime as base for GPU support
# The application will automatically detect and use available GPUs
FROM nvidia/cuda:12.3.0-runtime-ubuntu22.04

# Install Python 3.10 and system dependencies
RUN apt-get update -y && apt-get install -y \
    python3.10 \
    python3-pip \
    libegl1 libgl1 libxrender1 \
    netcat iputils-ping curl telnet iproute2 net-tools dnsutils tcpdump vim \
    && apt-get clean && rm -rf /var/lib/apt/lists/*

ARG dpath=dockerfiles/tmsserver
# Copy environment.yml and create the environment
# COPY ${dpath}/environment.yml /tmp/environment.yml
# RUN conda env create -f /tmp/environment.yml

# # Make sure conda activates the environment
# SHELL ["conda", "run", "-n", "TMS", "/bin/bash", "-c"]
# RUN pip install vtk
# RUN apt-get update && apt-get install -y python3 python3-pip
RUN ln -s /usr/bin/python3 /usr/bin/python
# Install Python packages with CUDA support for GPU access
# PyTorch with CUDA 12 support - will auto-detect GPU at runtime
RUN pip install --no-cache-dir \
    vtk pyigtl nibabel && \
    pip install --no-cache-dir --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio 2>&1 | grep -v "Hashes\|does not match" || true




# ...existing code...

# ...existing code...
ARG spath=SlicerTMS
COPY ${spath}/server /app/server
# COPY ${spath}/data /app/data
RUN mkdir -p /app/data
COPY test_port_connectivity.py /app/test_port_connectivity.py
COPY test_network_debug.sh /app/test_network_debug.sh
WORKDIR /app

# Make test scripts executable
RUN chmod +x /app/test_network_debug.sh

EXPOSE 18944
EXPOSE 18945

# Run the Python script
# The application automatically detects available GPUs at runtime
# Build: docker build -t tmsserver -f ./dockerfiles/tmsserver/dockerfile .
# Run with GPU: docker run --gpus all -p 18944:18944 -p 18945:18945 -it tmsserver
# Run without GPU: docker run -p 18944:18944 -p 18945:18945 -it tmsserver
CMD [ "python", "server/server.py", "Example4"]